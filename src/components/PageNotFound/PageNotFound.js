import React from 'react';
import './index.scss'

const PageNotFound = () => {
    return (
        <div className="notFound">
            This page does not exist
        </div>
    );
};

export default PageNotFound;
